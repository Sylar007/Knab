C# 10 - Global using
With C# 10 you can use global using directives that the compiler will consider for the whole project:

Global usings intend to simplify and lighten C# code, by declaring only once a using keyword on a specific namespace on a same project, then you won’t need to declare in another file which needs the same namespace